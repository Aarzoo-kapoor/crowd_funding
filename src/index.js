import React from 'react';
import ReactDOM from 'react-dom';
import Payment from './pages/Payment'; // Adjust the path if necessary

// Render the AboutUs component directly
ReactDOM.render(
  <React.StrictMode>
    <Payment/>
  </React.StrictMode>,
  document.getElementById('root')
);
